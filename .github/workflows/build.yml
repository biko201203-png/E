name: Build BONELAB Mod

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'

    - name: Download All Mod Dependencies
      run: |
        mkdir Refs
        curl -L -o bonelab_refs.zip "https://github.com"
        powershell Expand-Archive -Path bonelab_refs.zip -DestinationPath Refs_Temp -Force
        powershell "Get-ChildItem -Path Refs_Temp -Filter *.dll -Recurse | Move-Item -Destination Refs -Force"
        curl -L -o Refs\LabFusion.dll "https://github.com"
        curl -L -o Refs\BoneLib.dll "https://github.com"
      shell: cmd

    - name: Auto-Generate Project File
      run: |
        $projectXml = @"
        <Project Sdk="Microsoft.NET.Sdk">
          <PropertyGroup>
            <TargetFramework>net6.0</TargetFramework>
            <ImplicitUsings>enable</ImplicitUsings>
            <Nullable>enable</Nullable>
          </PropertyGroup>
          <ItemGroup>
            <PackageReference Include="LavaGang.MelonLoader" Version="0.6.1" />
            <PackageReference Include="UnityEngine" Version="1.0.0" />
            <Reference Include="Refs\*.dll" />
          </ItemGroup>
        </Project>
        "@
        Set-Content -Path "Mod.csproj" -Value $projectXml
      shell: powershell

    - name: Compile Mod DLL
      run: dotnet build Mod.csproj --configuration Release --output ./build

    - name: Upload Finished DLL
      uses: actions/upload-artifact@v4
      with:
        name: Compiled-Mod-DLL
        path: ./build/*.dll
